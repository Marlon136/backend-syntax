services:
  - type: web
    name: syntax-backend
    env: node
    plan: free
    buildCommand: "npm install --production=false && npm run build"
    startCommand: "npm run start:prod"
    envVars:
      - key: NODE_ENV
        value: production
        sync: false
